import os
import geos_ats
from geos_ats.test_builder import TestDeck, RestartcheckParameters, generate_geos_tests

restartcheck_params = {}
restartcheck_params['atol'] = 1.0E-8
restartcheck_params['rtol'] = 1.0E-8

decks = [
    TestDeck(name='dfgMovingGrid',
             description='Tests damage field partitioning and moving background grid using the mpm solver.',
             partitions=((2, 2, 1),),
             restart_step=1951,
             check_step=3970,
             restartcheck_params=RestartcheckParameters(**restartcheck_params)),
    TestDeck(name='singleParticle',
             description='Test that a single particle is set up properly and does nothing with the solid mechanics mpm solver on one rank.',
             partitions=((1, 1, 1),),
             restart_step=486,
             check_step=972,
             restartcheck_params=RestartcheckParameters(**restartcheck_params)),
    TestDeck(name='particleRepartition',
             description='Test that a single particle can properly transit between partitions using the mpm solver.',
             partitions=((2, 2, 1),),
             restart_step=1941,
             check_step=3882,
             restartcheck_params=RestartcheckParameters(**restartcheck_params))
        ]

generate_geos_tests(decks)