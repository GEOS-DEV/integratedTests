restartcheck_params = {}
restartcheck_params["atol"] = 1.0E-4
restartcheck_params["rtol"] = 2.0E-5

# format: (<input>, <desc>, <partition_list>, <restart_step>, <total_steps>)
decks = ((
    "elas3D_firstOrder_abc_smoke", "Elastic wave solver, first-order FE, first order formulation, absorbing BC",
    ((1, 1, 1), (2, 2, 2)), 100, 200
), ("elas3D_firstOrder_fs_smoke",
    "Elastic wave solver, first-order FE, first order formulation, absorbing BC on the sides, free surface BC at the top",
    ((1, 1, 1), (2, 2, 2)), 100, 200),
         (
    "elas3D_Q3_firstOrder_abc_smoke", "Elastic wave solver, third-order FE, first order formulation, absorbing BC",
    ((1, 1, 1), (2, 2, 2)), 100, 200
), ("elas3D_Q3_firstOrder_fs_smoke",
    "Elastic wave solver, third-order FE, first order formulation, absorbing BC on the sides, free surface BC at the top",
    ((1, 1, 1), (2, 2, 2)), 100, 200))

import geos_ats 
from geos_ats.test_builder import generate_geos_tests_from_values

generate_geos_tests_from_values( decks, restartcheck_params )