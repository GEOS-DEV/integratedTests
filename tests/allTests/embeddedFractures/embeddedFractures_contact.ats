# format: (<input>, <desc>, <partition_list>, <restart_step>, <total_steps>, <restartcheck_params>)

decks = (("EmbFrac_Compression_Frictionless", "Single efem fracture under compression - frictionless",
          ((1, 1, 1), (2, 2, 1)), 0, 1, {
              "atol": 1.0E-6,
              "rtol": 4.0E-7
          }), ("EmbFrac_Compression_CoulombFriction", "Single efem fracture under compression - Coulomb friction",
               ((1, 1, 1), (2, 2, 1)), 0, 1, {
                   "atol": 1.0E-6,
                   "rtol": 4.0E-7
               }))

import geos_ats 
from geos_ats.test_builder import generate_geos_tests_1

generate_geos_tests_1( decks, restartcheck_params )