
import os
import geos_ats
from geos_ats.test_builder import TestDeck, RestartcheckParameters, generate_geos_tests

restartcheck_params = {'atol': 1e-08, 'rtol': 4e-07}

decks = [TestDeck(name='Sneddon_smoke', 
                  description='Sneddon with horizontal fracture', 
                  partitions=((1, 1, 1), (2, 2, 1)), 
                  restart_step=0, 
                  check_step=1), 
         TestDeck(name='Sneddon_staticCondensation_smoke', 
                  description='Sneddon with horizontal fracture usic static condensation', 
                  partitions=((1, 1, 1), (2, 2, 1)), 
                  restart_step=0, 
                  check_step=1), 
         TestDeck(name='SneddonRotated_smoke', 
                  description='Sneddon with inclined fracture', 
                  partitions=((1, 1, 1), (2, 2, 1)),
                  restart_step=0, 
                  check_step=1)]

generate_geos_tests(decks, restartcheck_params)
